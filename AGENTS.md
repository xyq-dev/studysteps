# StudySteps 协作约定

## 适用范围

本文件适用于当前项目根目录及其全部子目录。StudySteps 是独立项目，不得复用或接入 Yuwang／渔网杯的账号、订单、支付数据、业务权限、数据库或素材空间。

当前工作区处于项目接手与任务拆分阶段。开始任何任务前，先阅读：

1. `docs/PROJECT_PLAN.md`：产品、业务、数据、接口、架构、验收与分期的基准方案。
2. `docs/CURRENT_STATUS.md`：当前事实、已完成项、未完成项和最近验证证据。
3. `docs/TASKS.md`：实施顺序、每项任务的修改范围和验收门槛。
4. 当前任务单及与其直接相关的专题文档。

`docs/PROJECT_PLAN.docx` 是保留的原始方案文件，`docs/PROJECT_PLAN.md` 是便于协作和审查的等义转换稿。不得为方便实现而静默改变其中已经确定的产品或架构结论。发现冲突时先记录差异并请求确认，不另建一套平行方案。

## 已确定的产品和架构边界

- P0 首发为手机优先 H5，兼容平板；管理后台为桌面网页。
- 前端采用 React、TypeScript、Vite；API 采用 NestJS、TypeScript；数据采用 PostgreSQL、Prisma。
- 服务端是模块化单体。后台任务首期使用单体工作进程与数据库 Outbox，不拆微服务，不引入专用队列、搜索集群或向量数据库。
- 规划目录为 `apps/web`、`apps/admin`、`apps/api`、`packages/contracts`、`packages/domain`、`packages/ui`、`prisma`、`docs`。
- 业务 API 使用 HTTPS、JSON 和 `/v1` 路径；契约、校验规则与错误码集中维护。
- P0 不包含照片、外部 AI、支付、题库、公开社区、陌生人私信、班级管理、直播课程或奖励商城。
- 学习时长只是辅助记录，不能自动决定任务完成、奖励或学习效果。
- 当前任务不授权创建生产环境、部署、迁移真实用户数据、邀请真实家庭或开通收费。

## 不得破坏的业务约束

- 所有学习数据归属具体 `StudentProfile`；每次对象访问都检查当前账号或设备会话与该档案的有效授权关系，不能只凭 `family_id` 放行。
- 年级与年龄分开建模。年龄未确认时不能创建正式学习记录；未满 14 岁档案必须满足适用的监护人告知、同意和版本留痕要求。
- 计划规则、每日任务实例和完成记录分别建模。规则变更不得覆盖已经发生或完成的历史。
- `TaskOccurrence` 使用稳定 ID；改期保留最初安排日期和发生键。生成任务的定时入口与读取补齐入口共享唯一约束。
- 完成、撤销、建议接受和离线重放必须幂等。一次完成所需的状态、记录、汇总和事件在同一事务边界内形成一致结果。
- 写入使用版本进行乐观锁；并发冲突返回明确的 409，不静默采用最后写入覆盖。
- 时间戳存 UTC；学习日保存本地日期和当时的时区快照。补记同时保留实际学习日与服务端接收时间。
- 一名学生最多一个未结束的学习会话。客户端计时仅负责展示，计时结束不能自动完成任务。
- 复盘正文默认学生个人可见；分享给监护人和“请家长帮忙”必须是明确动作。
- 管理员使用独立账号与权限入口；日志和审计记录不得包含访问令牌、儿童复盘正文等敏感内容。

## 工作方式

开始修改前：

1. 输出并核验实际工作目录、根目录文件和相关子目录。
2. 核验是否存在 Git 仓库；存在时记录当前分支、HEAD 和工作区状态，不存在时明确写“无分支、无 HEAD”，不得虚构。
3. 选择一个 `STP` 任务，确认其前置条件、允许修改范围、禁止范围和验收项。
4. 保留所有既有文件及用户改动；范围外文件只读检查，不顺手重构。

实施时：

- 只完成当前任务，不提前夹带后续 STP、P1 或 P2 功能。
- 复用 `packages/contracts`、`packages/domain` 和 `packages/ui` 的明确边界，不在三个应用内复制相互漂移的规则。
- 数据库约束、服务端授权和事务一致性不能用前端隐藏、客户端校验或测试桩替代。
- 版本、依赖和供应商只在对应任务中根据实际兼容性选择并记录；方案未指定的内容不得假装已经确定。
- 迁移必须可审查并说明回滚或前滚策略。未经明确授权，不执行生产迁移或部署。
- 不使用真实儿童信息、真实联系方式或生产凭证作为开发、种子或测试数据。

完成任务时：

1. 实际运行与风险相称的格式检查、类型检查、单元测试、集成测试和构建；未执行项明确标为“未执行”，不能写成通过。
2. 更新 `docs/CURRENT_STATUS.md` 和 `docs/TASKS.md` 的真实状态与证据；不要把“已设计”“已创建文件”写成“功能已验收”。
3. 实施报告必须包含任务编号、实际目录、分支与前后 HEAD、改动原因、修改文件、用户可见变化、迁移与回滚情况、执行的命令和结果、保留的既有改动、未解决问题。
4. 达到当前任务验收门槛后停止，不自动进入下一任务。

## Git 与外部操作

- Git 初始化、建分支、commit、push、建远程仓库或 PR，均以当前任务的明确授权为准；没有授权就不执行。
- 不修改远程配置，不强推，不重写他人历史。
- 不部署测试或生产环境，不发送通知，不调用真实短信、存储或分析供应商，除非任务明确授权并提供安全的环境边界。

## 项目命令

STP 003 已固定以下根命令；本地与 CI 使用同一组，不要另写跳过检查的脚本。

```bash
pnpm install --frozen-lockfile
pnpm lint
pnpm typecheck
pnpm test
pnpm build
pnpm prisma:validate
```

开发：

```bash
pnpm dev
pnpm dev:web
pnpm dev:admin
pnpm dev:api
```

包管理器为 pnpm `10.17.0`，目标 Node 为 `22`（`engines.node`: `>=22.14.0`）。版本选择、端口和 Docker 限制见根 `README.md`。
